{
  "name": "@restorecommerce/scheduling-srv",
  "version": "0.1.0",
  "description": "Restore Commerce job scheduling microservice",
  "main": "start.js",
  "author": "Invend GmbH",
  "repository": {
    "type": "git",
    "url": "https://github.com/restorecommerce/scheduling-srv.git"
  },
  "license": "MIT",
  "keywords": [
    "restore",
    "commerce",
    "microservice",
    "job",
    "scheduling"
  ],
  "dependencies": {
    "@restorecommerce/chassis-srv": "github:restorecommerce/chassis-srv",
    "@restorecommerce/cluster-service": "^0.1.0",
    "@restorecommerce/grpc-client": "github:restorecommerce/grpc-client",
    "@restorecommerce/kafka-client": "^0.1.0",
    "@restorecommerce/logger": "^0.1.0",
    "@restorecommerce/resource-base-interface": "github:restorecommerce/resource-base-interface.git",
    "@restorecommerce/service-config": "^0.1.0",
    "cache-manager": "^2.1.2",
    "cache-manager-redis": "^0.5.0",
    "co": "^4.6.0",
    "co-emitter": "^0.2.3",
    "kue": "^0.11.1",
    "kue-scheduler": "^0.7.4",
    "lodash": "^4.14.1"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.39",
    "@types/node": "^4.0.35",
    "@types/should": "^11.2.0",
    "co-mocha": "^1.1.3",
    "coveralls": "^3.0.0",
    "istanbul": "^0.4.4",
    "mocha": "^4.0.1",
    "mocha-typescript": "^1.1.4",
    "nsp": "^2.6.1",
    "should": "^11.2.1",
    "tslint": "^5.3.2",
    "typescript": "^2.3.4",
    "typescript-require": "^0.2.9"
  },
  "scripts": {
    "start": "npm run createtopics && node start.js",
    "dev": "export NODE_ENV=development; npm run createtopics && node start.js",
    "postinstall": "tsc -d",
    "pretest": "tsc -d && npm run tsctests && npm run createtopics",
    "tsctests": "tsc test/*.ts --module commonjs -target ES6",
    "createtopics": "node setupTopics.js io.restorecommerce.jobs io.restorecommerce.jobs.resource io.restorecommerce.command",
    "test": "npm run lint && npm run mocha",
    "test-debug": "npm run mochadebug",
    "lint": "tslint './*.ts'",
    "mocha": "export NODE_ENV=test; mocha -R spec ./test/service_test.js ./test/jobs_grpc_test.js ./test/jobs_kafka_test.js --full-trace --exit",
    "mochadebug": "export NODE_ENV=test; mocha -R spec ./test/*.js --inspect-brk --full-trace --exit",
    "coveralls": "export NODE_ENV=test; istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec ./test/service_test.js ./test/jobs_grpc_test.js ./test/jobs_kafka_test.js --exit"
  },
  "engines": {
    "node": ">= 9.2.0"
  }
}
